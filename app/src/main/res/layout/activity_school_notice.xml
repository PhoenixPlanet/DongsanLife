<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_school_notice"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.example.user.projects_team.SchoolNoticeActivity"
    android:background="#c1c4c1">

    <ListView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:id="@+id/notice_view"
        android:divider="@null"
        android:dividerHeight="5dp"
        android:layout_margin="4dp"
        android:listSelector="@android:color/transparent"
        android:cacheColorHint="@android:color/transparent"
        android:choiceMode="singleChoice"
        android:overScrollMode="ifContentScrolls"
        android:layout_alignParentTop="true"
        android:layout_alignParentStart="true" />

    <Button
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:id="@+id/nextNoticeButton"
        android:background="@drawable/next_button"
        android:layout_alignBottom="@+id/notice_view"
        android:layout_alignEnd="@+id/notice_view"
        android:layout_marginEnd="40dp"
        android:layout_marginBottom="20dp"/>

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/preNoticeButton"
        android:layout_alignBottom="@+id/notice_view"
        android:layout_alignLeft="@+id/notice_view"
        android:layout_marginLeft="40dp"
        android:layout_marginBottom="20dp"/>

</RelativeLayout>
